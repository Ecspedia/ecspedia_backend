spring.application.name=ecspedia
spring.datasource.url=jdbc:postgresql://localhost:5432/ecspedia
spring.datasource.username=ecspedia
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

#Hotel API -- Deprecated (configure via environment variables)
hotelbeds.api.base-url=${HOTELBEDS_API_BASEURL:https://api.test.hotelbeds.com/hotel-api/1.0}
hotelbeds.api.key=${HOTELBEDS_API_KEY:}
hotelbeds.api.secret=${HOTELBEDS_API_SECRET:}
hotelbeds.api.timeout=${HOTELBEDS_API_TIMEOUT:5000}
hotelbeds.api.enabled=${HOTELBEDS_API_ENABLED:true}

#Lite API overrides
liteapi.api.base-url=https://api.liteapi.travel/v3.0
liteapi.api.key=${LITEAPI_API_KEY}


# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=frojas.12122@gmail.com
spring.mail.password=crvj tpkm xqhz fkby
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Email Templates
app.email.from=noreply@ecspedia.com
app.email.from.name=Ecspedia Team
app.email.base-url=${BASE_URL:http://localhost:3000}

spring.graphql.http.path=/graphql
spring.graphql.graphiql.enabled=true
jwt.secret=secret
jwt.expiration=86400000

# Google Custom Search (set via environment variables in production)
google.search.api-key=${GOOGLE_SEARCH_API_KEY:}
google.search.cx=${GOOGLE_SEARCH_CX:}
google.search.num-results=${GOOGLE_SEARCH_NUM_RESULTS:3}

# AWS S3 (configure via environment variables)
aws.s3.bucket=${AWS_S3_BUCKET:}
aws.access-key-id=${AWS_ACCESS_KEY_ID:}
aws.secret-access-key=${AWS_SECRET_ACCESS_KEY:}
aws.s3.region=${AWS_S3_REGION:us-east-1}
aws.s3.prefix=${AWS_S3_PREFIX:hotel-images/}
